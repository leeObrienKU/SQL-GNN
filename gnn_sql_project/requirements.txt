# Core dependencies
torch
torch-geometric
psycopg2-binary
pandas
networkx
tabulate

# PostgreSQL server & client (for local/Colab setup)
# These are system packages, not pip packages, so install via apt in setup_env.sh
# postgresql
# postgresql-contrib
# postgresql-client

# Optional but required for NeighborSampler & accelerated PyG ops
pyg-lib
torch-scatter
torch-sparse
torch-cluster
torch-spline-conv